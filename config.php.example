<?php
// Пример конфигурационного файла
// Скопируйте этот файл в config.php и заполните своими данными

// Email получателя (адрес, на который будут приходить заявки с сайта)
$username = 'your-email@example.com';

// Пароль (может использоваться для SMTP аутентификации)
$password = 'your-password';

// Настройки SMTP для production (раскомментируйте и заполните для использования внешнего SMTP сервера)
// define('SMTP_HOST', 'smtp.example.com');        // SMTP сервер (например: smtp.gmail.com, smtp.mail.ru)
// define('SMTP_PORT', 587);                       // Порт (587 для TLS, 465 для SSL, 25 для без шифрования)
// define('SMTP_SECURE', 'tls');                   // Тип шифрования: 'tls', 'ssl' или '' (без шифрования)
// define('SMTP_AUTH', true);                      // Требуется ли аутентификация: true или false
// define('SMTP_USERNAME', 'your-email@example.com');  // Логин для SMTP (обычно email)
// define('SMTP_PASSWORD', 'your-password');           // Пароль для SMTP

// Альтернативно можно использовать переменные окружения вместо define():
// SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_AUTH, SMTP_USERNAME, SMTP_PASSWORD

// Примеры популярных почтовых сервисов:
// Gmail: SMTP_HOST='smtp.gmail.com', SMTP_PORT=587, SMTP_SECURE='tls', SMTP_AUTH=true
// Mail.ru: SMTP_HOST='smtp.mail.ru', SMTP_PORT=465, SMTP_SECURE='ssl', SMTP_AUTH=true
// Yandex: SMTP_HOST='smtp.yandex.ru', SMTP_PORT=465, SMTP_SECURE='ssl', SMTP_AUTH=true

